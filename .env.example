# GitTrack Discord Bot environment configuration
# Copy this file to .env and fill in the required values.

# Required: Discord application credentials
DISCORD_TOKEN=your_discord_bot_token_here
CLIENT_ID=your_discord_bot_client_id_here

# Required: Database connection
# Docker dev default (app inside container):
DATABASE_URL=postgresql://gittrack:password@postgres:5432/gittrack

# Required for /setup webhook instructions
# Preferred: set the full webhook endpoint explicitly.
WEBHOOK_URL=http://localhost:3000/github-webhook

# Optional fallback base URL used only when WEBHOOK_URL is not set.
# The bot will derive: ${PUBLIC_URL}/github-webhook
# PUBLIC_URL=http://localhost:3000

# Optional: Express server port (default: 3000)
# PORT=3000

# Optional: Limits
MAX_REPOS_ALLOWED=10
MAX_NOTIFICATION_CHANNELS_ALLOWED=unlimited

# Optional: Used for private-repository branch autocomplete in /link
# GITHUB_TOKEN=your_github_personal_access_token_here

# Notes:
# - WEBHOOK_URL takes precedence when both WEBHOOK_URL and PUBLIC_URL are set.
# - Webhook secrets are generated per repository by /setup.
